#include "main.h"

/**
 * _strncpy - copying str into other
 * @dest: destination
 * @src: source
 * @n: lenght
 * Return: char *
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
